# Unit testing code that uses JoinableTaskFactory

The application is responsible for creating a singleton `JoinableTaskContext`
that is associated with its main thread and `SynchronizationContext`.
For unit tests

